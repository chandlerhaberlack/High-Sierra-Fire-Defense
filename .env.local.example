# Formspree form ID for the early access waitlist
# https://formspree.io/f/meewyvkr
NEXT_PUBLIC_FORMSPREE_ID=meewyvkr

# Google Maps JavaScript API key for the wildfire history map (RiskMap section).
# When unset, the site falls back to the embedded ArcGIS dashboard.
# Create a key: https://console.cloud.google.com/google/maps-apis/credentials
# Enable the "Maps JavaScript API", then restrict the key to your domain(s).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
